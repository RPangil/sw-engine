const SearchBar = () =>
{

    const searchHandler = () =>
    {

    }

    return (
        <form>
            <input type="search" />
            <button type="button" onClick={searchHandler}>Search</button>
        </form>
    )
}

export default SearchBar;